// 01 — Try/Catch básico con errores sincrónicos
// Objetivo: practicar detección y manejo de errores sin cortar la app (JSON.parse).
// Referencia: try/catch y flujo al catch cuando hay error. 

// TODO 1: implementá parsearUsuario(str) que haga JSON.parse(str) dentro de try/catch.
//  - si OK, retorná el objeto;
//  - si falla con SyntaxError, retorná null y logueá un mensaje amigable.
// TODO 2: probá con JSON válido e inválido.

console.log("Completá parsearUsuario(str) y probá ambos casos.");